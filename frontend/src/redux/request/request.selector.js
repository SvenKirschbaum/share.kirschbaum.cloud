export const selectRequest = (state) => state.request.request;
export const selectRequestId = (state) => state.request.id;
export const selectRequestLoadingState = (state) => state.request.state;
export const selectRequestError = (state) => state.request.error;
export const selectRequestUploadState = (state) => state.request.uploadState;
